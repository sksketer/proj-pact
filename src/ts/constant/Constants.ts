export enum CanvasConstants {
    myCanvas = "myCanvas",
    canvasWidth = 1280,
    canvasHeight = 720,
}

export enum Constants {
    // Number-constants
    HUNDERED = "100",
    
    // Loader
    loader = "loader",
    loadingMeter = "loading",

}
